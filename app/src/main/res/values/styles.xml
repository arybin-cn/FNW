<resources>

    <declare-styleable name="ds">
        <attr name="colorBlurOverlay" format="color" />
        <attr name="colorTextTranslucent" format="color" />
    </declare-styleable>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#3b1373</item>
        <item name="colorPrimaryDark">#230a45</item>
        <item name="colorAccent">#FF4081</item>
        <item name="colorBlurOverlay">#1A000000</item>
        <item name="colorTextTranslucent">#bfffffff</item>
        <item name="android:textColor">#efffffff</item>
        <item name="android:clipChildren">false</item>
        <item name="android:clipToPadding">false</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
