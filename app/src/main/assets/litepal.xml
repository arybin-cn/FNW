<?xml version="1.0" encoding="utf-8"?>
<litepal>

    <dbname value="default_lite" />

    <!--
        Define the version of your database. Each time you want
        to upgrade your database, the version tag would helps.
        Modify the models you defined in the mapping tag, and just
        make the version value plus one, the upgrade of database
        will be processed automatically without concern.
            For example:
        <version value="1" />
    -->
    <version value="9" />

    <!--
        Define your models in the list with mapping tag, LitePal will
        create tables for each mapping class. The supported fields
        defined in models will be mapped into columns.
        For example:
        <list>
            <mapping class="com.test.model.Reader" />
            <mapping class="com.test.model.Magazine" />
        </list>
    -->
    <list>
        <mapping class="info.arybin.fearnotwords.model.orm.Plan"/>
        <mapping class="info.arybin.fearnotwords.model.orm.Expression"/>
        <mapping class="info.arybin.fearnotwords.model.orm.Entity" />
        <mapping class="info.arybin.fearnotwords.model.orm.Pronounce" />
        <mapping class="info.arybin.fearnotwords.model.orm.Translation" />
        <mapping class="info.arybin.fearnotwords.model.orm.ExpressionL" />
        <mapping class="info.arybin.fearnotwords.model.orm.EntityL" />
    </list>


</litepal>